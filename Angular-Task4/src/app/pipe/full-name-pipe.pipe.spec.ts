import { FullNamePipePipe } from './full-name-pipe.pipe';

describe('FullNamePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FullNamePipePipe();
    expect(pipe).toBeTruthy();
  });
});
